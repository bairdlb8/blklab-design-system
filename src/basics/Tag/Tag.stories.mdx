import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'

import Tag from './Tag'

<Meta title="Basics / Tag" component={ Tag } />

# Tag

## Add some text here

export const Template = (args) => {
  return <Tag {...args} />
}

<Preview withToolbar>
  <Story name="Primary" args={ {} }>
    {Template.bind({})}
  </Story>
</Preview>

<Props of={ Tag } />
