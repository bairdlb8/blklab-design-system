import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'

import Text from './Text'

<Meta title="Basics / Text" component={ Text } />

# Text

## Add some text here

export const Template = (args) => {
  return <Text {...args} />
}

<Preview withToolbar>
  <Story name="Primary" args={ {} }>
    {Template.bind({})}
  </Story>
</Preview>

<Props of={ Text } />
