import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'

import ChoiceList from './ChoiceList'

<Meta title="Basics / ChoiceList" component={ ChoiceList } />

# ChoiceList

## Add some text here

export const Template = (args) => {
  return <ChoiceList {...args} />
}

<Preview withToolbar>
  <Story name="Primary" args={ {} }>
    {Template.bind({})}
  </Story>
</Preview>

<Props of={ ChoiceList } />
