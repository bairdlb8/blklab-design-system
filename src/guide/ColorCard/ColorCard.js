import React from 'react'
import PropTypes from 'prop-types'

import './ColorCard.scss'

const ColorCard = ({className=''}) => {
  return <div>ColorCard</div>
}

ColorCard.propTypes = {}

export default ColorCard
