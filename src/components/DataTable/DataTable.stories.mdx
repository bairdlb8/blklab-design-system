import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'

import DataTable from './DataTable'

<Meta title="Features / DataTable" component={DataTable} />

# DataTable

export const Template = (args) => {
  return <DataTable {...args} />
}

<Preview>
  <Story
    name="DataTable"
    args={{
      className: '',
      headers: {
        title: { title: 'Title', sortDirections: ['ascending', 'descending'] },
        something: { title: 'Something' }
      },
      data: [
        { title: 'Home', something: 'Testing' },
        { title: 'Products', something: 'Testing' },
        { title: 'About', something: 'Testing' }
      ]
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

<Props from={DataTable} />
