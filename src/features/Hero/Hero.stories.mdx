import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'

import Hero from './Hero'
import Image from '../../basics/Image'
import Icon from '../../basics/Icon'
import Button from '../../basics/Button'
import Container from '../../containers/Container'

<Meta title="Features / Hero" component={Hero} />

# Hero

export const Template = (args) => {
  return (
    <Hero {...args}>
      <Container
        maxWidth="800px"
        className="display-flex flex-dir-col justify-content-center align-items-center"
      >
        <p className="text-align-center light-text mb-xl">
          The rational model is based on a rationalist philosophy According to the rationalist
          philosophy, design is informed by research and knowledge in a predictable and controlled
          manner.
        </p>
        <Button className="mx-auto" icon="chevron-right">
          Join Today
        </Button>
      </Container>
    </Hero>
  )
}

<Preview>
  <Story
    name="Hero"
    args={{
      className: '',
      backgroundImage: 'https://picsum.photos/seed/picsum/1200/600',
      title: 'Home Page'
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

<Props from={Hero} />
