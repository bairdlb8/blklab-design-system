import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'

import Banner from './Banner'

<Meta title="Features / Banner" component={ Banner } />

# Banner

## Add some text here

export const Template = (args) => {
  return <Banner {...args} />
}

<Preview withToolbar>
  <Story name="Primary" args={ {} }>
    {Template.bind({})}
  </Story>
</Preview>

<Props of={ Banner } />
