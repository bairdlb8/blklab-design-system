import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'

import SixtyFourty from './SixtyFourty'

<Meta title="Layouts / SixtyFourty" component={ SixtyFourty } />

# SixtyFourty

## Add some text here

export const Template = (args) => {
  return <SixtyFourty {...args} />
}

<Preview withToolbar>
  <Story name="Primary" args={ {} }>
    {Template.bind({})}
  </Story>
</Preview>

<Props of={ SixtyFourty } />
