import React from 'react'
import PropTypes from 'prop-types'

import './Editorial.scss'

const Editorial = ({className=''}) => {
  return <div>Editorial</div>
}

Editorial.propTypes = {}

export default Editorial
