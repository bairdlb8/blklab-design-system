import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'

import Logout from './Logout'

<Meta title="Templates / Logout" component={ Logout } />

# Logout

## Add some text here

export const Template = (args) => {
  return <Logout {...args} />
}

<Preview withToolbar>
  <Story name="Primary" args={ {} }>
    {Template.bind({})}
  </Story>
</Preview>

<Props of={ Logout } />
