import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks'

import Login from './Login'

<Meta title="Templates / Login" component={Login} />

# Login

## Add some text here

export const Template = (args) => {
  return <Login {...args} />
}

<Preview>
  <Story name="Example" args={{}}>
    {Template.bind({})}
  </Story>
</Preview>

<Props of={Login} />
